# alineferezin.github.io

Site profissional da **Aline Ferezin**, psicóloga clínica (TCC). Publicado via
GitHub Pages em https://alineferezin.github.io (domínio próprio depois).

**Estado:** base preparada. A página inicial (`/`) é um placeholder na marca
dela, já no ar. O site completo ainda vai ser construído — com **propostas**
pra ela escolher antes.

## Pra continuar (nova sessão do Claude)

Leia, nesta ordem:
1. **`CLAUDE.md`** — contexto da Aline, regras (mobile-first, propostas antes de
   escolher, `/` sempre funcional) e o fluxo.
2. **`PRODUCT.md`** — registro/estratégia (rascunho, confirmar com ela).
3. **`docs/BUILD-TOOLKIT.md`** — como construir com o acabamento do portfólio do
   Silas (fontes base64, screenshots headless, PDF, foto, publicação).

Materiais da Aline (bio, fotos, site atual) ficam em `materials/` (local, fora
do git). Inspiração de qualidade: `../silasAssisMachado.github.io`.
