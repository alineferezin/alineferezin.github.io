# alineferezin.github.io

Site profissional da **Aline Ferezin**, psicóloga clínica (TCC). Publicado via
GitHub Pages em https://alineferezin.github.io (domínio próprio depois).

**Estado:** só a **base (documentação)** está pronta. A página inicial (`/`) é
um **placeholder neutro** (sem design/marca aplicados de propósito), só pra
funcionar no Pages. O site de verdade — stack e visual — é construído pela
**próxima sessão**, com **propostas** pra Aline escolher antes.

## Pra continuar (nova sessão do Claude)

Leia, nesta ordem:
1. **`CLAUDE.md`** — contexto da Aline, regras (mobile-first, propostas antes de
   escolher, `/` sempre funcional, stack em aberto) e o fluxo.
2. **`PRODUCT.md`** — registro/estratégia (rascunho, confirmar com ela).
3. **`docs/BRAND.md`** — identidade oficial: paleta exata + fontes.
4. **`docs/BUILD-TOOLKIT.md`** — técnicas reaproveitáveis do portfólio do Silas
   (fontes base64, screenshots headless, PDF, foto, publicação, bugs).

Materiais da Aline (bio, fotos, site atual) ficam em `materials/` (local, fora
do git). Inspiração de qualidade: `../silasAssisMachado.github.io`.
